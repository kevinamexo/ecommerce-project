import React from "react";
import "./Loader.css";

const Loader = () => {
  return (
    <div className="loader" style={{ background: "red", width: "100%" }}>
      Loading..
    </div>
  );
};

export default Loader;
